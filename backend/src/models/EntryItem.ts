export interface EntryItem {
  userId: string
  entryId: string
  createdAt: string
  name: string
  entryDate: string
  done: boolean
  location: string
  architect: string
  attachmentUrl?: string
}
